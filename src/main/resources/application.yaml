spring:
  application:
    name: flash-sale-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/flash_sale_db
    username: postgres
    password: 12345

  jpa:
    hibernate:
        ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

